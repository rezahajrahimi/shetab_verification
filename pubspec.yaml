name: shetab_verification
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  another_telephony: ^0.3.0  # بروزرسانی به آخرین نسخه
  permission_handler: ^11.0.1  # بروزرسانی به نسخه سازگار با اندروید 15
  workmanager: ^0.5.2  # بروزرسانی
  shared_preferences: ^2.2.2
  dio: ^5.4.0
  infinite_scroll_pagination: ^4.0.0
  mobile_scanner: ^6.0.7
  jalali_table_calendar: ^1.3.5
  cupertino_icons: ^1.0.8
  flutter_easyloading: ^3.0.5
  

dev_dependencies:
  flutter_launcher_icons: ^0.14.3
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_launcher_icons:
    android: "launcher_icon"
    ios: true
    image_path: "assets/icon/icon.png"
flutter:
  uses-material-design: true
